# 2단계: 설계 — 완료 기준 체크리스트

**작성 시각:** 2026-06-09-2033
**대상 프로젝트:** 일정 관리툴 v2 재설계 — 상주 FTXUI TUI (CLI_Planning)
**핸드오프:** `CLI_Planning/code-design/02-design/handoff.md` (현재 WIP — 이 체크리스트 승인 후 상단 WIP 마크 제거하여 확정)

## 완료 기준

- [x] 적용 흐름(A/B)이 명시됨. B인 경우 variant 파일의 세부 케이스도 추가 명시됨
      → 흐름 B (B3: 기능 추가 + 리팩터링). handoff 1장에 사유 명시
- [x] 모듈 경계와 책임이 명확히 정의됨
      → 3장 — orchard/CLI_Planning(trunk/roots/leaves/glass) + toolshed(sqlite/log) 역할 모듈 + 책임 표
- [x] 모듈 간 의존이 단방향이고 순환 의존이 없음
      → 3-5 mermaid 다이어그램. core(trunk) ← 어댑터 단방향, toolshed는 orchard 미import
- [x] 주요 인터페이스(함수 시그니처, API)가 명세됨
      → 4장 — 4-1 불변(코어, archive 참조) / 4-2 충돌 2-phase / 4-3 toolshed/sqlite / 4-4 toolshed/log / 4-5 TuiApp / 4-6 파서 보류
- [x] 외부 시스템 연동이 있으면 타임아웃/재시도/에러 처리/mock 전략이 명세됨
      → 4-3 toolshed/sqlite (WAL/foreign_keys/busy_timeout PRAGMA, in-memory mock), 6장(운영). 코어 외부연동은 v1 동일(archive 6장)
- [x] 데이터 흐름이 다이어그램으로 정의됨
      → 5장 — 5-1 뼈대 / 5-2 충돌 2-phase / 5-3 입력검증 / 5-4 에러모델 / 5-5 포커스, mermaid + 표
- [x] 비기능 요구사항 각 항목에 대한 설계 반영 방안이 작성됨
      → 6장 — 성능/보안/호환/운영 v1 유지 + 확장성(NF3 어댑터) + 재사용성(toolshed 격리) 신규
- [x] TDD 계획(테스트 케이스 목록)이 happy path / 실패 / edge case로 분류되어 작성됨
      → 8장 — 8-A 회귀 가드 / 8-B 신규 단위 4종(CreateEvent·UpdateEvent 2-phase·toolshed sqlite·log) / 8-C 수동 QA(qa-checklist.md)
- [x] 기능 추가인 경우 영향 범위가 파악되어 기록됨
      → 4-1(불변)/4-2(변경)/4-5(신규)로 영향 구분. ConflictPrompter 제거가 CLI main 경로에 반영(9-v2)
- [x] 리팩터링인 경우 현재 구조 분석 + 전환 경로 + 회귀 방지 테스트 목록이 작성됨
      → 2장(as-built, `analyze/facet/`) + 7장(Phase A A0~A6 / Phase B B1~B4, 매 단계 138 green) + 8-A(회귀 가드)
- [x] 설계 결정 기록에 채택 사유와 대안 검토가 포함됨
      → 9장 — v1 결정 이어받음 + v2 18개 결정 행(각 채택/사유/기각 대안)
- [x] 모호한 사용자 표현은 모두 되묻고 명확화됨
      → 11장 — v1 #1~16 + v2 #17~35 명확화 기록
- [x] 임의 결정이 없음 (모든 결정은 사용자 확인됨)
      → v2 모든 결정(UI 형태/입력/디렉토리/충돌/네임스페이스/인터페이스/TDD 전략) 사용자 확인 받음
- [x] 반복 작업의 경우 깊이 +1 / +2 기준에 맞게 조사가 이루어짐
      → 재설계(같은 프로젝트 2회차)라 깊이 가중. code-analyze facet 산출물을 1차 입력으로 사용
- [x] N/A로 표기된 항목이 모두 사용자 확인을 받음
      → 아래 "N/A 항목 확인" 섹션에서 확인
- [x] 피드백 영역이 채워짐 (없어도 N/A 명시)
      → 검토 대기 중. 아래 "피드백" 섹션에 결과 기입 예정

## N/A 항목 확인

다음 항목이 N/A 로 표기됨. **사용자 확인 완료 (2026-06-09): 4개 모두 N/A 맞음.**

- **비기능: 보안 (인증/인가)**: N/A — 로컬 1인 도구, 외부 시스템 격리 (01 명시, v1 유지) [확인됨]
- **비기능: 확장성 (사용자)**: N/A — 1인 영구 (01 명시). 단 어댑터 확장성(NF3)은 6장에 해당 [확인됨]
- **비기능: 가용성**: N/A — 1인 로컬 도구, 다운타임 개념 없음 [확인됨]
- **인터페이스 명세: 자연어 파서**: N/A(의식적 보류) — seam=기존 Command, LLM 단계로 연기 (4-6, 9장 기록) [확인됨]

## 피드백
N/A — 사용자 검토 결과 피드백 없음 (2026-06-09). 02 설계 확정.
