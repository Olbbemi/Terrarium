# 2단계: 설계 — 완료 기준 체크리스트

**작성 시각:** 2026-05-31-1940
**대상 프로젝트:** CLI 기반 일정 관리툴 (Kyklos Gaia / CLI_Planning)
**핸드오프:** `CLI_Planning/code-design/02-design/handoff.draft.md` (체크리스트 승인 후 `handoff.md` 로 rename)

## 완료 기준

- [x] 적용 흐름(A/B)이 명시됨. B인 경우 variant 파일의 세부 케이스도 추가 명시됨
      → 흐름 A (새 프로젝트). B variant 해당 없음
- [x] 모듈 경계와 책임이 명확히 정의됨
      → 8개 모듈 (`domain` / `application` / `ports` / `adapter_sqlite` / `adapter_cli` / `adapter_logger` / `adapter_config` / `main.cpp`) 책임 표 작성
- [x] 모듈 간 의존이 단방향이고 순환 의존이 없음
      → 3-3 mermaid 다이어그램 + 의존 방향 규칙 명시. core ← adapter 단방향, main.cpp 만 Composition Root
- [x] 주요 인터페이스(함수 시그니처, API)가 명세됨
      → Domain Entity 4종 + Domain Service + Ports 6 인터페이스 모두 시그니처 작성. Application Layer UseCase 패턴 + Command/Result 예시
- [x] 외부 시스템 연동이 있으면 타임아웃/재시도/에러 처리/mock 전략이 명세됨
      → 4-6 표 — SQLite (busy_timeout 5초, 자동 재시도, 도메인 예외 변환, in-memory mock), 파일 시스템, stdin/stdout 모두 명세
- [x] 데이터 흐름이 다이어그램으로 정의됨
      → `flows/` 디렉토리 4개 시나리오 (`01-event-add-with-conflict`, `02-todo-done-interactive`, `03-show-dashboard`, `99-error-handling`) + 5-2 상태 변경 지점 표
- [x] 비기능 요구사항 각 항목에 대한 설계 반영 방안이 작성됨
      → 6장 — NF1~NF7 + 보안/확장성/가용성/호환성/운영/빌드 표 작성
- [x] TDD 계획(테스트 케이스 목록)이 happy path / 실패 / edge case로 분류되어 작성됨
      → 8장 — Domain (5 단위) / Application (16 UseCase) / Adapter (7 어댑터) / E2E (3 시나리오) 4 표
- [x] 기능 추가인 경우 영향 범위가 파악되어 기록됨
      → 신규 프로젝트이므로 해당 없음 (N/A)
- [x] 리팩터링인 경우 현재 구조 분석 + 전환 경로 + 회귀 방지 테스트 목록이 작성됨
      → 신규 프로젝트이므로 해당 없음 (N/A)
- [x] 설계 결정 기록에 채택 사유와 대안 검토가 포함됨
      → 9장 3 표 (Aggregate/Domain, 라이브러리, 인프라). 각 결정마다 사유 + 검토 후 채택 안 한 대안 명시
- [x] 모호한 사용자 표현은 모두 되묻고 명확화됨
      → 11장 — 15개 명확화 기록
- [x] 임의 결정이 없음 (모든 결정은 사용자 확인됨)
      → 모든 결정 (Aggregate β, UseCase 16개, 라이브러리 9개, D2/D3 9개) 사용자 확인 받음
- [x] 반복 작업의 경우 깊이 +1 / +2 기준에 맞게 조사가 이루어짐
      → 처음 다루는 영역 (표준 깊이), 사전 조사 6개 카테고리 각 3~5회 검색 수행
- [x] N/A로 표기된 항목이 모두 사용자 확인을 받음
      → 아래 "N/A 항목 확인" 섹션에서 확인
- [x] 피드백 영역이 채워짐 (없어도 N/A 명시)
      → 사용자 답변: N/A (피드백 없음)

## N/A 항목 확인

다음 항목이 N/A 로 표기됨. 정말 해당 없음이 맞는지 확인 부탁드립니다:

- **현재 상태**: N/A — 신규 프로젝트이므로 기존 코드 없음
- **변경 영향 범위 (기능 추가)**: N/A — 신규 프로젝트
- **전환 경로 (리팩터링)**: N/A — 신규 프로젝트
- **비기능: 보안 (인증/인가)**: N/A — 로컬 1인 도구, 외부 시스템 격리 (01 명시)
- **비기능: 확장성 (사용자)**: N/A — 1인 영구 (01 명시)
- **비기능: 가용성**: N/A — 1인 로컬 도구, 다운타임 개념 없음
- **B variant 세부 케이스**: N/A — 흐름 A 선택

## 피드백

N/A — 사용자 답변 "일단 모두 N/A" (2026-05-31-1940).

N/A 항목 7개 모두 사용자 확인 완료, 추가 피드백 없음.
